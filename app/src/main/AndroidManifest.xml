<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.blaxtation.inviteaguest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/main_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/main_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Bookings"
            android:label="@string/title_activity_bookings">

        </activity>
        <activity android:name=".GuestCategory" />
        <activity android:name=".AllTalents" />
        <activity android:name=".RequestStatus" />
        <activity android:name=".guestListafterCategory" />
        <activity android:name=".invite" />
        <activity android:name=".contact_us" />
        <activity android:name=".about_us" />
        <activity android:name=".faq" />
        <activity android:name=".gallery" />
        <activity
            android:name=".HomeScreenNavDrawer"
            android:label="@string/title_activity_home_screen_nav_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GuestDescription" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>